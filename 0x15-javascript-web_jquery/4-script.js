$('DIV#toggle_header').click(function () {
  $('header').toggleClass('red green');
});
// When the user clicks on DIV#toggle_header, the class of the HTML tag HEADER must be toggled between red and green. If the current class is red, the class must be updated to green, and vice-versa.
